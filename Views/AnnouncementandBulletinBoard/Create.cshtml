@model TisaWasteManagement.Models.Announcement

@{
    ViewData["Title"] = "New Announcement";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section Styles {
    <link rel="stylesheet" href="~/css/AnnouncementandBulletinBoardStyles.css" />
}

<div class="module-page">

    <!-- ===== PAGE HEADER ===== -->
    <div class="page-header">
        <div>
            <h1 class="page-title">
                <i class="bi bi-plus-circle"></i> New Announcement
            </h1>
            <p class="page-subtitle">Create a new announcement for residents</p>
        </div>
        <div class="page-actions">
            <a asp-action="Index" class="btn btn-cancel">
                <i class="bi bi-arrow-left"></i> Back to List
            </a>
        </div>
    </div>

    <!-- ===== FORM ===== -->
    <div class="module-card">
        <div class="module-card-body">
            <form asp-action="Create" method="post" enctype="multipart/form-data">

                <div asp-validation-summary="ModelOnly" class="alert alert-danger-custom"></div>

                <div class="form-row">
                    <div class="form-group">
                        <label asp-for="Title" class="form-label">
                            Title <span class="required">*</span>
                        </label>
                        <input asp-for="Title" class="form-control" placeholder="Enter announcement title" />
                        <span asp-validation-for="Title" class="text-danger"></span>
                    </div>
                </div>

                <div class="form-group">
                    <label asp-for="Content" class="form-label">
                        Content <span class="required">*</span>
                    </label>
                    <textarea asp-for="Content" class="form-control" rows="5" placeholder="Enter announcement content..."></textarea>
                    <span asp-validation-for="Content" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label class="form-label">Picture <span class="text-muted">(Optional)</span></label>
                    <input type="file" name="imageFile" class="form-control" accept=".jpg,.jpeg,.png,.gif" />
                    <div class="form-text">Accepted formats: JPG, PNG, GIF. Max size 5 MB.</div>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-check-lg"></i> Post Announcement
                    </button>
                    <a asp-action="Index" class="btn btn-cancel">
                        <i class="bi bi-x-lg"></i> Cancel
                    </a>
                </div>

            </form>
        </div>
    </div>

</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}